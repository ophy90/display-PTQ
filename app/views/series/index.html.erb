<p id="notice"><%= notice %></p>

<h1>Mêlées - <%= @event.name %></h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @series.each do |series| %>
      <tr>
       <td> <%= link_to 'show', club_event_series_path(@club, @event, series) %> </td>
        <!-- link_to 'Show', club_event_series_path(@club, @event, @serie) -->
        <!-- link_to 'Edit', edit_series_path(series) -->
        <!-- link_to 'Destroy', series, method: :delete, data: { confirm: 'Are you sure?' } -->
      </tr>
    <% end %>

    <%= link_to 'New Serie', new_club_event_series_path %>
  </tbody>
</table>

<br>

